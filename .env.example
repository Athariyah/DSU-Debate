# Frontend uses the Vite proxy by default. Set absolute URLs when frontend
# and backend are served from different origins in production.
VITE_API_URL=/api
VITE_SOCKET_URL=
VITE_BACKEND_URL=http://localhost:4000
VITE_USE_MOCKS=false
